---
# SSH user (must have sudo access)
ansible_user: root

# Q1 application
q1_version: "0.1.0-SNAPSHOT"
q1_jar_local: "{{ playbook_dir }}/../q1-api/target/q1-api-{{ q1_version }}.jar"
q1_port: 9000
q1_raft_port: 6000
q1_data_dir: /var/lib/q1/data

# Erasure coding (0 = disabled)
q1_ec_k: 2   # data shards
q1_ec_m: 1   # parity shards  (k+m must be <= number of nodes)

# EC repair scanner
q1_repair_interval_s: 60
q1_repair_batch_size: 200

# Peer list — format: id:host:httpPort:raftPort per node, comma-separated
# Built from the inventory: each node in group [q1] contributes one entry.
q1_peers: >-
  {{ groups['q1']
     | map('regex_replace', '^(.*)$',
           '\1:' + (hostvars['\1']['ansible_host'] | default('\1'))
           + ':' + q1_port|string + ':' + q1_raft_port|string)
     | join(',') }}
