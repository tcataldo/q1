<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.q1</groupId>
  <artifactId>q1</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Q1 Object Store</name>
  <description>S3-compatible object store optimised for small-file workloads</description>

  <modules>
    <module>q1-core</module>
    <module>q1-erasure</module>
    <module>q1-cluster</module>
    <module>q1-api-grpc</module>
    <module>q1-api</module>
    <module>q1-tests</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>25</java.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>

    <!-- runtime deps -->
    <undertow.version>2.3.18.Final</undertow.version>
    <ratis.version>3.1.0</ratis.version>
    <slf4j.version>2.0.16</slf4j.version>
    <logback.version>1.5.12</logback.version>

    <!-- test deps -->
    <awssdk.version>2.30.0</awssdk.version>
    <junit.version>5.11.4</junit.version>

    <!-- storage -->
    <rocksdb.version>9.7.3</rocksdb.version>

    <!-- gRPC / protobuf -->
    <grpc.version>1.65.1</grpc.version>
    <protobuf.version>3.25.3</protobuf.version>

    <!-- plugins -->
    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    <maven-failsafe-plugin.version>3.5.2</maven-failsafe-plugin.version>
    <exec-maven-plugin.version>3.5.0</exec-maven-plugin.version>

    <!-- test infra -->
    <testcontainers.version>1.20.4</testcontainers.version>

    <!-- coverage -->
    <jacoco.version>0.8.14</jacoco.version>

    <!--
      Default value for the argLine property injected by jacoco:prepare-agent.
      Without this, @{argLine} in Surefire/Failsafe would be unresolved when
      the JaCoCo plugin is not bound (e.g. during plain `mvn test -pl q1-core`
      without the full reactor).
    -->
    <argLine></argLine>

    <!-- resilience -->
    <resilience4j.version>2.2.0</resilience4j.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- internal modules -->
      <dependency>
        <groupId>io.q1</groupId>
        <artifactId>q1-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.q1</groupId>
        <artifactId>q1-erasure</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- persistent index -->
      <dependency>
        <groupId>org.rocksdb</groupId>
        <artifactId>rocksdbjni</artifactId>
        <version>${rocksdb.version}</version>
      </dependency>
      <dependency>
        <groupId>io.q1</groupId>
        <artifactId>q1-api-grpc</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.q1</groupId>
        <artifactId>q1-api</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- gRPC BOM -->
      <dependency>
        <groupId>io.grpc</groupId>
        <artifactId>grpc-bom</artifactId>
        <version>${grpc.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- protobuf runtime -->
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${protobuf.version}</version>
      </dependency>

      <!-- HTTP server -->
      <dependency>
        <groupId>io.undertow</groupId>
        <artifactId>undertow-core</artifactId>
        <version>${undertow.version}</version>
      </dependency>

      <!-- internal modules -->
      <dependency>
        <groupId>io.q1</groupId>
        <artifactId>q1-cluster</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- cluster coordination (Raft) -->
      <dependency>
        <groupId>org.apache.ratis</groupId>
        <artifactId>ratis-server</artifactId>
        <version>${ratis.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.ratis</groupId>
        <artifactId>ratis-grpc</artifactId>
        <version>${ratis.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.ratis</groupId>
        <artifactId>ratis-metrics-default</artifactId>
        <version>${ratis.version}</version>
      </dependency>


      <!-- logging -->
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>

      <!-- AWS SDK v2 BOM (test / compliance) -->
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${awssdk.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- rate limiting -->
      <dependency>
        <groupId>io.github.resilience4j</groupId>
        <artifactId>resilience4j-ratelimiter</artifactId>
        <version>${resilience4j.version}</version>
      </dependency>

      <!-- testing -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- Testcontainers BOM -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${java.version}</release>
            <enablePreview>true</enablePreview>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
          <configuration>
            <!-- @{argLine} is expanded late so jacoco:prepare-agent can inject its -javaagent -->
          <argLine>@{argLine} --enable-preview --enable-native-access=ALL-UNNAMED</argLine>
          </configuration>
        </plugin>

        <!-- integration tests (IT suffix) wired to verify phase -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${maven-failsafe-plugin.version}</version>
          <configuration>
            <argLine>@{argLine} --enable-preview --enable-native-access=ALL-UNNAMED</argLine>
          </configuration>
          <executions>
            <execution>
              <goals>
                <goal>integration-test</goal>
                <goal>verify</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>${exec-maven-plugin.version}</version>
        </plugin>

        <!--
          JaCoCo: instrument unit tests (prepare-agent) and integration tests
          (prepare-agent-integration).  Reports are generated at the verify phase.
          Modules activate this plugin by referencing it in their own <plugins> section.
          The aggregate cross-module report is produced by q1-tests.
        -->
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.version}</version>
          <executions>
            <execution>
              <id>prepare-agent</id>
              <goals><goal>prepare-agent</goal></goals>
            </execution>
            <execution>
              <id>prepare-agent-integration</id>
              <goals><goal>prepare-agent-integration</goal></goals>
            </execution>
            <execution>
              <id>report</id>
              <phase>verify</phase>
              <goals><goal>report</goal></goals>
            </execution>
            <execution>
              <id>report-integration</id>
              <phase>verify</phase>
              <goals><goal>report-integration</goal></goals>
            </execution>
          </executions>
        </plugin>

      </plugins>
    </pluginManagement>
  </build>

</project>
